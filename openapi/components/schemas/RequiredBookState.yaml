allOf:
  - $ref: ./BookState.yaml
  - type: object
    required:
      - statistics
    properties:
      statistics:
        required:
          - reading_status
      location:
        required:
          - source
