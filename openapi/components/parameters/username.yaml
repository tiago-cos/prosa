name: username
in: query
required: false
schema:
  type: string
description: |
  Restricts the results to one user's items.

  Required for regular users, who may only search their own; optional for
  admins, who see everything when it is left out.
example: "silveira12"
